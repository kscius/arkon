# Iteración QA — 2026-06-24 15:19

## Resumen
- pass: 73 | parcial: 0 | fail: 0 | skip: 0
- % completitud: 100%

## Blockers (arreglar primero)
| ID | Gap | Causa probable | Fix propuesto | Archivos |
|----|-----|--------------|---------------|----------|
| — | — | — | — | — |

## Major / Minor
| ID | Gap | Severidad | Fix aplicado |
|----|-----|-----------|--------------|
| E2E suite | `TENANT_ID` no definido → credenciales ARKON en Docker CONAGUA | blocker | Default `TENANT_ID=conagua` en `playwright.config.ts` |
| FLOW-01 / SOL-01 | Segunda corrida E2E sin borrador en seed | major | `ensureBorradorSolicitud()` en `e2e/helpers.ts` |
| COPY-01 alerts | Texto Validación/liberación ausente tras mutar alertas | minor | Navegar a `/alertas` + regex ampliado |
| RBAC municipal | Timeout en heading durante carga | minor | Timeout 15s + `domcontentloaded` en `rbac-routes.spec.ts` |
| Login E2E | `page.goto` flaky bajo carga | minor | Retry ×3 + `waitUntil: domcontentloaded` |

## Plan de la siguiente iteración
1. N/A — criterio DONE cumplido.

## Casos que pasaron a pass en esta iteración
- Suite E2E completa (45/45) en dos corridas consecutivas sin reset de seed entre ellas.
- Fixes de infraestructura E2E (tenant, idempotencia FLOW-01, estabilidad login).

## Validación automática
| Comando | Resultado |
|---------|-----------|
| `pnpm test` | 19/19 pass |
| `pnpm exec tsc -b` | pass |
| `pnpm lint` | pass (1 warning preexistente ObraFormModal) |
| `pnpm test:e2e` ×2 | 45/45 + 45/45 |
| Docker health | `localhost:8080/api/health` ok |

## Browser MCP (confirmación)
- AUTH-01: login one-click con email prellenado
- REG-01: `/obras/1` mensaje amigable (no 500)
- SHELL-04: 12 ítems sidebar estatal
- REG-03: campana con contador alertas
